pipeline{
    agent any
    tools{
        gradle 'gradle 6.3'
    }
    stages{
        stage("Build jar"){
            steps{
                echo 'building the staging application...'
                sh './gradlew clean build'
            }
        }
        
       stage("Build docker"){
            steps{
                echo 'building docker...'
                sh 'docker build -f src/main/docker/Dockerfile -t cms-api:latest .' 
                sh 'docker tag cms-api dzioba/cms-api:latest'
            }
        }
        
       stage("Publish image to Docker Hub"){
            steps{
                echo 'publishing docker...'
                withDockerRegistry([ credentialsId: "docker-hub-credentials", url: "" ]) {
                    sh 'docker push dzioba/cms-api:latest'
                }
            }
        }
        stage('Run Docker container') {
            steps {
                echo 'running docker container...'
                gcloud compute scp --recurse --zone=europe-west3-c boot $2:/home/dzioba99/cms-api/
                gcloud compute ssh --zone=europe-west3-c cms-api-prod --command="sudo sh ~/boot/run.sh $3 $4 $5"
            }
        }
    }
}
