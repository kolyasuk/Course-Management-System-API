pipeline{
    agent any
    tools{
        gradle 'Gradle'
    }
    stages{
        stage("build"){
            steps{
                echo 'building the staging application...'
                sh './gradlew clean build'
            }
        }

        stage("test"){
            steps{
                echo 'testing the staging application...'
            }
        }

        stage("deploy"){
            steps{
                echo 'deploying the staging application...'
            }
        }
    }
}
